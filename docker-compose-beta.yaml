services:
  postgres:
    container_name: tutorops-postgres-beta
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_DB_USER}
      POSTGRES_PASSWORD: ${POSTGRES_DB_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB_NAME}
    ports:
      - "5433:5432"
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
      - ./backend/migrations:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    container_name: tutorops-redis-beta
    image: redis:7-alpine
    restart: unless-stopped
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    command:
      - sh
      - -c
      - exec redis-server /usr/local/etc/redis/redis.conf --requirepass "$$REDIS_PASSWORD"
    volumes:
      - ./configs/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - ./data/redis:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$$REDIS_PASSWORD\" ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  backend:
    container_name: tutorops-backend-beta
    build:
      context: ./backend
    restart: unless-stopped
    env_file:
      - ./configs/.env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      default: {}
      edge-network:
        aliases:
          - tutorops-backend-beta

  frontend:
    container_name: tutorops-frontend-beta
    build:
      context: ./frontend
      args:
        VITE_API_URL: /api
        VITE_GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
    restart: unless-stopped
    ports:
      - "${NGINX_HOST_PORT:-8080}:80"
    networks:
      default: {}
      edge-network:
        aliases:
          - tutorops-frontend-beta

  backup:
    container_name: tutorops-backup-beta
    build:
      context: .
      dockerfile: backup/Dockerfile
    restart: unless-stopped
    env_file:
      - ./configs/.env
    environment:
      POSTGRES_DB_HOST: ${POSTGRES_DB_HOST}
    depends_on:
      postgres:
        condition: service_healthy

networks:
  default: {}
  edge-network:
    external: true
